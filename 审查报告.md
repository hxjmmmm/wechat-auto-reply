# 微信自动回复助手 —— 现状梳理 · 代码检测 · 问题清单

审查时间：2026-09-17
审查范围：项目根目录全部 Python 源码 + `build_exe.py` 打包清单 + `data/` 与 `dist/data/` 运行日志 + 分享包
代码规模：14 个 .py，约 4100 行；全部通过 `py_compile` 语法检查，**语法层面无错**

---

## 一、现在的逻辑（当前实际行为）

### 1.1 一个 exe，三种身份（`launcher.py`）

```
微信自动回复助手.exe                  → gui.main()          开界面
微信自动回复助手.exe --daemon-run     → daemon.main()       后台常驻
微信自动回复助手.exe --script <名> -- <参数...>
                                      → runpy 跑伴随脚本    （拿自己当解释器）
```

`launcher.py` 在 import 期先把 `sys.stdout/stderr` 垫上（`_attach` 按 fd 1/2 重新打开），
否则 `--noconsole` 打包后任何摸 `sys.stdout.buffer` 的模块都会当场崩。

### 1.2 常驻监听闭环（`daemon.py` → `monitor.py` → `reply_engine.py` → `send_v2.py`）

```
每 poll_seconds 秒一轮：
 daemon.loop_once()
   ├─ monitor.lock_touch() / self_lock_touch()     写心跳，防多实例
   ├─ monitor.run_once()                           纯 Python 读库，0 token
   │    ├─ wxenv.run_cli(history …)   → wechat-cli.exe 或内置 wechat_cli 包
   │    ├─ 消息行 sha1 指纹 与 state.json 比对 → 新的「她发的」消息
   │    ├─ 首次运行只建基线（baseline_ready），历史消息一条不回
   │    ├─ 语音：voice_text.py 读 packed_info_data 转写；没出来 → voice_pending
   │    ├─ 图片：decrypt_image.py 解 .dat → data/images/
   │    └─ 写入 pending.json（防丢消息队列）
   ├─ 判断唤醒原因：new fresh>0 / voice ready>0 / pending 且距上次 ≥pending_retry
   ├─ reply_engine.generate()  → 一次 HTTP（OpenAI 兼容）→ clean_lines → data/reply.txt
   └─ send_v2.py：随机等 20~90s → 点搜索框 → 贴名字 → 回车 → 拆条真实键鼠发送
        └─ verify_sent.py：发送后读 Msg_<md5(wxid)> 表，确认真的落在她的会话
             ├─ 命中 → clear_pending() → 统计 sent+1
             └─ 未命中 → 保留队列，300 秒后重发
```

### 1.3 关键设计（这部分是好的）

- **空闲 0 token**：等待在脚本里做，模型只在真有消息时被调一次 HTTP。
- **三态读消息全离线**：文字直读库；图片解 `.dat`；语音读库里的转写文本 —— 都不碰屏幕，息屏可用。
- **锁屏安全闸**：点完搜索框检查前台窗口，没到就 `stage: abort`，一个字不发（返回码 2）。
- **待回复队列**：检测到即入 `pending.json`，发送确认成功才清，防止「检测到但没发出去」被静默吞掉。
- **配置与数据落在 exe 同级**（`runner.app_dir()`），不用 `_MEIPASS`，避免每次启动配置丢失。
- **打包自包含**：`wechat_cli` / `Crypto` / `uiautomation` / `comtypes` / `zstandard` 全 collect-all，
  目标机不装 Python 也能跑；`wxenv.run_cli` 还有「外部 exe 失败 → 内置包回落」的兜底。

### 1.4 打包后的实际运行证据

`dist/data/selftest.txt` 显示：`frozen=True`、`app_dir=dist\`、伴随脚本都在 `%TEMP%\_MEI00003eac2\`。
`dist/data/daemon.log` 显示 exe 版监听跑起来了、检测到新消息、LLM 调用成功（HTTPS 正常）。
**但整条链路在「发送后校验」这一环断了 —— 见 A1。**

---

## 二、问题清单

### A 级：直接影响正常使用

#### A1（最致命）打包后「发送成功」永远被判为失败 → 消息被反复重发

- **位置**：`send_v2.py:219` `script = os.path.join(HERE, "verify_sent.py")`
- **根因**：`HERE = runner.app_dir()`，打包后 = **exe 所在目录**（`dist\`）。
  而 `verify_sent.py` 被 PyInstaller 解压到 `%TEMP%\_MEIxxxx\` 里，**不在 exe 旁边**。
  实测 `dist\verify_sent.py` 不存在 → `send_v2.py:221` 的 `os.path.exists(script)` 为假 →
  `verify_after_send()` 直接返回 `{"ok": False, "error": "缺少 decrypt_python 或 verify_sent.py"}`。
- **放大路径**：
  `send_v2.py:424` → `stage = "sent-wrong-session"`，返回码 3
  → `daemon.py:245` 把它当「发错会话」
  → `daemon.py:297` 记 send_failed、**不清 pending**
  → `daemon.py:337` 每 `pending_retry`（默认 300 秒）再唤醒一次
  → 重新生成回复、**再发一遍同样的内容**
- **后果**：消息其实发出去了，但程序以为失败，于是**每隔 5 分钟给对方重复发一次**，
  日志里还会一直刷「⚠️ 可能发到别的会话去了，请人工检查」。这是最容易被感知为「用起来不正常」的现象。
- **附带**：`_shot.ps1` / `ocr.ps1` 也没进 `build_exe.py` 的 `SCRIPTS`（`build_exe.py:30-35`），
  所以 exe 里连截图 OCR 那条旧路也不可用，`--no-verify` 是唯一选择。
- **修法**：`verify_after_send` 改用 `runner.script_path("verify_sent.py")`；
  并把 `_shot.ps1`、`ocr.ps1`、`prompt.txt` 加进 `SCRIPTS` / `EXTRAS`。

#### A2 发送后校验依赖本机 venv，换台机器必炸

- **位置**：`send_v2.py:218` `py = cfg.get("decrypt_python")`；`verify_sent.py:104`
  `sys.path.insert(0, r"C:\Users\<你的用户名>\.workbuddy\skills\wechat-local-butler\tool")`
- **根因**：校验脚本必须用「装了 wechat_cli 的解释器」跑，代码里写死了一个绝对路径。
  别人的机器上既没有那个 venv，也没有那个目录 → 校验必然返回错误 → 触发 A1 的重复发送。
- **修法**：走 `runner.cmd_for(cfg, "decrypt_python", "verify_sent.py")`（打包后自动重入 exe）；
  `verify_sent.py` 里删掉硬编码，交给 `runner` / `wxenv` 定位。

#### A3 待回复队列被整体清空 → 语音消息被永久丢弃

- **位置**：`daemon.py:286`（草稿模式）与 `daemon.py:293`（发送成功）都是 `monitor.clear_pending()`
- **根因**：`monitor.clear_pending()` 直接删掉整个 `pending.json`。
  一轮检测里如果同时有「能回的文字」和「转写还没出来的语音」（后者被 `prep_result` 过滤掉、
  但仍在 pending 里），发送成功后连语音那条一起被清掉 —— 而它的指纹已经进了 `state.seen`，
  **再也不会被报出来**。
- **后果**：她发的那条语音永远不会被回复，且没有任何日志提示。
- **修法**：`clear_pending` 改成按指纹「移出本轮真正回复成功的那些」，而不是清空。

#### A4 单实例锁在轮询间隔 > 120 秒时失效 → 可能跑出两个守护进程

- **位置**：`monitor.py:493` `LOCK_STALE = 120`；`daemon.py:115` `(time.time() - hb) < 120`；`gui.py:139` 同样写死 120
- **根因**：心跳只在**每轮开头**写一次（`daemon.py:308-309`）。
  但一轮里可能串行包含：读库检测 ≤180s + 图片解密 ≤180s + 语音转写 ≤240s + 发送 ≤420s，
  单轮最坏十几分钟。GUI 的轮询间隔 Spinbox 又允许 **5～600 秒**（`gui.py:851`）。
- **后果**：只要间隔 >120s 或一轮耗时 >120s，
  GUI 会显示「○ 未运行」、点「启动监听」能再起一个；新进程的 `self_lock_alive()` 也返回 False。
  **两个守护进程同时盯同一个会话 → 每条消息被回复两次。**
- **修法**：阈值按间隔动态算（如 `max(120, interval * 3)`），或把 `daemon.lock` 的 pid 用
  `OpenProcess` 探活，不再依赖心跳时间。

---

### B 级：功能失效 / 配置无效 / 安全

#### B1 「有动作时弹桌面通知」开关是死的

`gui.py:842 / 1011 / 1036` 认真读写 `cfg["notify"]`，但 `daemon.py` **从来不读它** ——
`daemon.py:281` 是无条件 `notify.notify(...)`。
而且它只在「草稿模式」弹，真正发送成功时反而不弹。用户关掉这个开关不会有任何效果。

#### B2 分享包里的 config.json 带着真实 API Key 与本机绝对路径

`分享包/config.json` 现在是：
`api_key = aa0a…`（你的真 key）、`contact = 周超`、
`wechat_cli / decrypt_python / send_python` 全是 `C:\Users\<你的用户名>\...` 的绝对路径。
而 `gui.py:487 make_share_package()` 的设计是写**干净模板**（不含 key / 联系人 / 本机路径），
说明这份包是手工拷的 `dist/config.json`，不是按钮生成的。
**直接发出去就等于泄露 API Key，而且对方一打开全是死路径。**

#### B3 两份 config.json 的 `auto_send` 相反，容易误判

- 项目根 `config.json`：`auto_send = false`
- `dist/config.json`：`auto_send = true`
- `dist/data/daemon.log` 里 exe 版一直在打 `自动发送已关闭（草稿模式）` —— 即监听跑了、回复也生成了，
  但一条都没发出去。如果以为「程序坏了」，实际上只是这个开关为 false。

#### B4 「重建基线」与守护进程并发写 state.json

`gui.py:900 rebuild_baseline()` 在 GUI 进程内直接跑 `monitor.run_once(init=True)`。
此时 daemon 可能正在跑 → 两个进程同时写 `data/state.json`；
而 `monitor.py:111 save_json()` 用的是**固定**临时名 `path + ".tmp"`，
并发时 `os.replace` 可能抛 `FileNotFoundError/PermissionError`（这里没有 try 兜底）。
另外重建基线会 `clear_pending()`，正在排队待发的消息会被丢掉。

#### B5 停止响应可能拖十几分钟

`daemon.py:357-371` 的 `stop_requested()` 只在**一轮结束后**检查。
如果当前正卡在 wechat-cli（180s×N）、解密（180s）、语音（240s）或发送（420s）里，
要等整轮跑完才退出。而 GUI 提示的是「最多等一个轮询周期」，与实际不符。

---

### C 级：逻辑漏洞 / 边界情况

| 编号 | 位置 | 问题 |
|---|---|---|
| C1 | `reply_engine.py:323-341` | 去重+截断代码**完全相同地写了两遍**，第二段永远无效，纯噪音 |
| C2 | `reply_engine.py:306-312` | 超长行拆分的兜底条件写反了：`>18 字` 才拆，但拆出的片段只要 `<=40 字` 就照单收下 → 一句 19~40 字、没有标点的长句会被**原样当成一条消息发出去**，绕过「每条 ≤12 字」的核心约束 |
| C3 | `reply_engine.py:158-164` | `style_text()` 是死代码，没有任何调用方；`STYLE.md` 只是被打包进去当文档 |
| C4 | `gui.py:799` | `self.cfg["prompt_file"]` 写进配置后再没人读，死配置 |
| C5 | `notify.py:16`、`send_v2.py:23-24`、`decrypt_image.py:48`、`voice_text.py:40`、`verify_sent.py:32` | 全部用 `io.TextIOWrapper(sys.stdout.buffer, …)` 直接包一层 —— `monitor.py:54-59` 已经明确写了**为什么不能这么干**（`--noconsole` 下 `sys.stdout` 为 None 会崩；包装器还会在解释器退出时先关掉底层流，再 flush 就报 `I/O operation on closed file`）。现在没炸只因为 `launcher.py` 提前垫了 stdout。应统一改成 `sys.stdout.reconfigure(...)` |
| C6 | `daemon.py:226` | `raw[raw.index("{"):]` 拿「第一个 `{`」开始切 JSON；send_v2 前面的日志行一旦含 `{` 就解析错。应只取最后一行以 `{` 开头的 |
| C7 | `monitor.py:232-248` | `is_from_her` 靠 `昵称: ` 前缀判断。群聊里消息形如 `[群名] 某人: 内容`，会被当成「我发的」，方向判断整体反掉 |
| C8 | `decrypt_image.py:92-97` vs `wxenv.py:254` | 两处对「wxid 目录名去后缀」的规则不一致（一个按段数、一个按正则），换账号命名后容易一边对一边错 |
| C9 | `monitor.py:377` | `state["seen"] = list(seen)[-MAX_SEEN:]`，`seen` 是从 set 重建的列表，截断顺序不严格等于新旧顺序；历史消息滑出窗口后可能被当成「新消息」重放。当前 `history_limit=60` / `MAX_SEEN=400` 风险低，调大 limit 就会踩 |
| C10 | `start-daemon.cmd` | 实测是**纯 LF 换行**（`xxd` 显示 `0a`，不是 `0d0a`）；GUI 生成的启动项用的是 CRLF。cmd.exe 对纯 LF 的批处理在 label/goto 场景会出问题，建议统一成 CRLF + GBK |

---

### D 级：体感 / 一致性

- `gui.py:459-460`：`_render_env()` 在**主线程**同步调 `wxenv.wechat_running()`（内部两次 `tasklist`，每次超时 15s）。
  开界面和点「自动检测」时极端情况下会卡住整个窗口。
- `gui.py:87`：通义千问预设是 `qwen-plus`，README 写的是 `qwen-flash`，文档与代码不一致。
- `watch.py` 已被 `daemon.py` 取代，但仍留在仓库且**没打进 exe**（不在 `build_exe.py` 的 `SCRIPTS` 里）
  → `--script watch.py` 会失败。
- README 里还整段保留着 `monitor.py --wait` 那套「定时任务叫醒模型」的旧方案说明，
  与当前 daemon 架构已经分叉，容易误导后来改代码的人。
- 项目根没有 `prompt.txt`，提示词只能从界面保存 —— 打包/分享时不会带上自定义人设。

---

## 三、修复优先级建议

| 顺序 | 动作 | 解决 |
|---|---|---|
| 1 | `send_v2.py` 用 `runner.script_path()` 定位 `verify_sent.py`；把 `_shot.ps1`/`ocr.ps1`/`prompt.txt` 加进打包清单；重新打包 | A1、B2 的重复发送根因 |
| 2 | `verify_sent.py` 去掉硬编码路径，改由 `runner` 定位 | A2 |
| 3 | `clear_pending` 改成按指纹精确移除 | A3 |
| 4 | 心跳阈值动态化 / 用 pid 探活 | A4 |
| 5 | daemon 里按 `cfg["notify"]` 决定是否弹通知 | B1 |
| 6 | 重新用「生成分享包」按钮产出干净 config，发出前检查 | B2 |
| 7 | 重建基线前先停 daemon；`save_json` 临时名带 pid | B4 |
| 8 | C1/C2/C5 顺手清一遍（都是几行的事） | C 级 |

## 三、修复记录（2026-09-17 已按上面顺序改完）

| 编号 | 改了什么 | 状态 |
|---|---|---|
| A1 | `send_v2.py` 新增 `_asset()`，`verify_after_send` 与 `_shot.ps1`/`ocr.ps1` 一律走 `runner.script_path`；`verify_after_send` 的解释器改由 `runner.cmd_for` 决定（打包后自动重入 exe）；跑之前先删掉旧的 `sent_check.json`；`build_exe.py` 新增 `ASSETS = ["_shot.ps1", "ocr.ps1"]` | ✅ 已改 + 已重新打包 |
| A2 | `verify_sent.py` 删掉 `C:\Users\<你的用户名>\...` 硬编码，改为 `_wechat_cli_dirs()` 多路径探测（`_MEIPASS` / 程序目录 / 由 `USERNAME` 与 `~` 推导的技能目录），导入失败时给出明确错误而不是静默崩 | ✅ 已改 |
| A3 | `monitor.clear_pending(fps=None)` 支持按指纹精确移除；`daemon.handle_new` 计算 `replied_fps` 并透传 | ✅ 已改 |
| A4 | `monitor` 新增 `pid_alive()` + `heartbeat_alive()`，`lock_touch(interval)` 记录轮询间隔；`daemon.self_lock_alive` 与 `gui.daemon_alive` 统一改成 pid 探活优先、心跳兜底（阈值 `max(180, 3×interval)`） | ✅ 已改 |
| B1 | `daemon` 新增 `_notify()`，按 `cfg["notify"]` 决定是否弹；发送成功时也会通知一次 | ✅ 已改 |
| B2 | `分享包/config.json` 换成干净模板（无 key、无联系人、无本机路径），与「生成分享包」按钮的语义一致 | ✅ 已改 |
| B4 | `monitor.save_json` / `gui.save_cfg` 临时文件名带 pid；「重建基线」检测到监听在跑会先停止并等它退出，等不到就提示稍后再试 | ✅ 已改 |
| C1 | 删掉 `clean_lines` 里重复的第二段去重代码 | ✅ 已改 |
| C2 | 新增 `MAX_PIECE = 18`：超长行先按标点拆，拆完仍超长就**硬切**，不再允许 19~40 字无标点的长句原样发出 | ✅ 已改 |
| C5 | `notify.py` / `send_v2.py` / `decrypt_image.py` / `voice_text.py` / `verify_sent.py` / `watch.py` 的 `io.TextIOWrapper(sys.stdout.buffer, …)` 全部换成 `sys.stdout.reconfigure(...)` | ✅ 已改 |
| C7 | `daemon.send_reply` 改为只解析「最后一行以 `{` 开头」的 JSON | ✅ 已改 |
| C10 | `start-daemon.cmd` 由 LF 换行转成 CRLF（内容仍是 GBK） | ✅ 已改 |
| C3/C4 | 删除死代码 `reply_engine.style_text()` 与死配置 `cfg["prompt_file"]` | ✅ 已改 |
| D | 通义千问预设模型由 `qwen-plus` 改成 README 里写的 `qwen-flash` | ✅ 已改 |

### 补充：实跑一轮又暴露两个（2026-09-17 12:51 实测发现，已修）

第一次真机跑 `daemon.py --once --no-send` 时直接失败：
`生成失败: HTTP 400: {"error":{"code":"1210","message":"图片输入格式/解析错误"}}`。
查下去是两层原因叠在一起，都会造成**消息卡在队列里每 5 分钟重试一次、永远发不出去**。

| 编号 | 问题 | 根因 | 修法 |
|---|---|---|---|
| A5 | 图片解密密钥用错了账号 code | `decrypt_image.find_code()` 用宽松正则 `key_(\d+)_` 在 `os.listdir` 的任意顺序里碰运气，命中了无关的 `key_0_4065598785_...` → `code=0` → `xor_key=0`、aes_key 全错 → 解出垃圾（`format: "bin"`） | 优先用 `config.account_code`，否则复用 `wxenv.find_account_code()`（严格正则 + 按时间戳排序）；最后才扫目录且 `0` 视为非法。修复后 `code=211958201`、`xor_key=185`，两张图正确解成 `.jpg` / `.png` |
| A6 | 垃圾文件被当成图片上报，视觉模型 400 后整轮卡死 | 解密结果 `format: "bin"` 也照样写盘、塞进 `new_images`；`reply_engine.has_image()` 只看文件存在 → 切视觉模型 → 智谱 4xx → `generate` 抛异常 → 队列不清 → 无限重试 | ① `decrypt_image` 只放行 jpg/png/gif/webp，其余记进 `skipped` 不上报；② `reply_engine` 新增 `_usable_image()` 按扩展名再筛一道；③ `generate` 里视觉模型失败自动降级为文本模型重试一次，最差也是"看不到图"的自然接话，不会一条都发不出 |

修复后同一条消息重跑：`LLM glm-4-flash 生成 3 条` → 草稿「宝宝, 累不累 / 休息会 / 我马上来」。

### 补充：exe 里「测试连接」必失败（2026-09-17 13:53 实测发现，已修）

源码方式跑 `daemon.py --test-llm` 一直返回 `{"ok": true}`，界面里点「测试连接」却必失败，
exe 实跑的报错是：

```
生成失败: URLError: <urlopen error unknown url type: https>
```

`unknown url type: https` 的意思不是连不上网，而是 **urllib 里根本没有 HTTPSHandler**。
顺着查下去是打包环节的问题：

| 编号 | 问题 | 根因 | 修法 |
|---|---|---|---|
| A7 | exe 里 `import ssl` 直接失败 → HTTPS 全部不可用 | PyInstaller 解析 `_ssl.pyd` 依赖时**按 PATH 顺序**找 `libssl-3-x64.dll`。本机 PATH 里 `C:\Program Files\HP\HP One Agent` 排在 `miniconda3\Library\bin` 前面，于是 HP 自带的 OpenSSL 3.0（1,290,280 字节）被打进 exe —— 而 `_ssl.pyd` 是按 OpenSSL **3.5.6** 编的，符号对不上 → `ImportError: DLL load failed while importing _ssl: 找不到指定的程序` → `ssl.py` 导入中断 → `urllib.request` 没有 `HTTPSHandler` → 所有 https 请求 `unknown url type` | ① `build_exe.py` 新增 `fix_dll_search_path()`：打包前把「当前解释器自己的 `Library\bin` / `DLLs` / 安装根」顶到 PATH 最前面，抓到的一定是配套那份；② `HIDDEN` 补上 `ssl`、`_ssl`（`ssl` 是 urllib 用到才 import 的，静态扫描抓不到）；③ `launcher.selftest()` 新增 ssl / HTTPS 两行，**打包完自动在 exe 里跑一次自检**，HTTPS 不通过就直接报「这个包不能用」 |

顺带修的两个打包坑：

- **旧 exe 删不掉导致打包崩在最后一步**：PyInstaller 收尾会 `os.remove` 同名旧 exe，
  在受限环境里删除会被安全策略拦住 → 整个打包 `rc=1`。改成先把旧 exe `os.replace` 成
  `.old`（重命名不是删除），让它无旧可删。
- **诊断脚本自身有 bug**：`dist/_ssltest.py` 里 `urllib.request._handlers` 是 Python 2 的
  私有属性，3.x 没有，诊断自己先崩了。已重写（改用 `HTTPSHandler` 探测 + 真实 HTTPS 请求）。

修复后实跑证据：

```
import ssl: OK, OpenSSL = OpenSSL 3.5.6 7 Apr 2026
HTTPSHandler: <class 'urllib.request.HTTPSHandler'>
GET https://open.bigmodel.cn/api/paas/v4/models -> HTTP 401   ← 握手+证书都过，只是没带 key

dist\微信自动回复助手.exe --daemon-run --once --no-send
[14:02:40] LLM glm-4-flash 生成 3 条
[14:02:40] 回复草稿：宝宝, 嗯😩 / 喝杯茶 / 睡一觉
```

同一条消息在 13:53 还是 `unknown url type: https`，14:02 已经正常出草稿。
新 exe（24.1 MB）已同步到 `分享包/`。

> 顺带确认：证书校验走 Windows 证书存储（`cert_store_stats: x509=241`），
> frozen 环境下依然可用，不需要额外打包 CA bundle。

### 补充：消息发出去了却报「可能发到别的会话」（2026-09-17 14:14 实测发现，已修）

现象：日志显示 `回复草稿：宝宝, 在么`，紧接着

```
发送未成功（sent-wrong-session）：⚠️ 可能发到别的会话去了，请人工检查
⚠️ 注意：消息可能发到了别的会话，待回复队列已保留，请人工确认后再处理
```

但微信里**消息明明已经发出去了**。而且队列保留 → 下一轮再发一遍 →
实测她连着收到「宝宝, 在呀」「宝宝, 在么」「宝宝, 在么？」三条。

`data/send_result.json` 里的真因是：

```json
"sent_check": {"ok": false, "error": "FileNotFoundError(2, 'No such file or directory')"}
```

**校验脚本自己崩了，压根没查过** —— 但上层把「校验出错」和「真发错会话」当成了同一件事。

| 编号 | 问题 | 根因 | 修法 |
|---|---|---|---|
| A8 | 打包后读库校验恒报 `FileNotFoundError` | exe 用**外部 python** 去跑 `_MEIPASS` 里的 `send_v2.py` / `verify_sent.py`。这种子进程的 `runner.app_dir()` 算出来的是 **%TEMP%\_MEIxxxx**（脚本所在临时目录），不是 exe 所在目录 → `load_cfg()` 读到空配置、`CONFIG_PATH` 指向 `_MEIPASS\config.json`（不存在）→ 一打开就 FileNotFoundError | `runner.app_dir()` 新增：父进程通过环境变量 `WX_APP_DIR` 指定时优先用它；`runner.child_env()` 自动把 `WX_APP_DIR` 传给子进程。daemon → send_v2 → verify_sent 整条链因此都能定位到真正的 `config.json` / `data` |
| A9 | 「校验出错」被当成「发错会话」 | `send_v2.py` 里 `stage = "sent" if chk.get("ok") else "sent-wrong-session"`，`chk` 里只要有 error 就落进 wrong-session；daemon 再据此**保留待回复队列** → 下一轮原样重发 | 新增 `sent-verify-error` 阶段：① 措辞改成「已发送，但读库复核没跑起来」；② daemon 把队列清掉（避免重复发送），只留一条让人确认一次的提示 |
| A10 | 出错看不出在哪一行 | `verify_sent.py` 只报 `f"{type(e).__name__}: {e}"`，就一句 `FileNotFoundError`，完全没法定位 | 异常里附带 `traceback.format_exc(limit=8)`，并单独存进 `traceback` 字段 |

修复后同一条调用链的实测（不发消息，只跑校验子链）：

```
app_dir  = D:\...\dist                 ← 不再跑到 %TEMP%
DATA_DIR = D:\...\dist\data
verify_after_send → {"ok": true, "matched": ["宝宝, 在么"], "error": null}
```

顺带清掉了当时卡在队列里的重复项，并停掉了会继续重发的旧守护进程。

> 另外发现：`分享包/config.json` 的 `api_key` 字段被人粘进了一段 Python traceback
> （就是上面 `_ssltest.py` 那个报错），已恢复成干净模板。

### 自检结果

22 项功能自检全部通过（`clear_pending` 精确移除、pid 探活、长句硬切、脚本定位、
notify 开关等），`py_compile` 全量通过，`launcher.py --selftest` 仍为 `RESULT: PASS`。
`verify_after_send` 实跑一次：返回 `error: null`、真正去查了库 —— 不再是一句
「缺少 decrypt_python 或 verify_sent.py」。

### 还没改（低优先级，需要你定夺）

- **C8**：`decrypt_image.clean_wxid` 与 `wxenv.list_accounts` 去 wxid 后缀的规则不一致
  （一个按段数切、一个按正则），换账号命名时可能一边对一边错。
- **C9**：`state.seen` 截断取 `list(seen)[-400:]`，顺序不完全等于新旧；
  当前 `history_limit=60` 不会踩，调大就要改。
- **D**：`watch.py` 已被 `daemon.py` 取代但仍留在仓库，且不在打包清单里。
- **D**：`gui._render_env` 仍在主线程同步跑 `tasklist`（上限 30 秒），极端情况会卡界面。
- README 里 `--wait` 那套旧方案的说明与现在的 daemon 架构已经分叉，建议后续一并重写。

---

## 四、验证方式

```bash
# 1. 已确认的事实
ls dist/            # → 没有 verify_sent.py / _shot.ps1 / ocr.ps1
cat dist/data/selftest.txt   # → app_dir=dist，脚本在 %TEMP%\_MEI00003eac2\

# 2. 复现 A1（打包后 --verify-after 必失败）
dist\微信自动回复助手.exe --script send_v2.py -- --contact 测试 --no-verify --verify-after \
    --message "ping" --yes
# 预期：输出里 sent_check.error = "缺少 decrypt_python 或 verify_sent.py"，返回码 3
#       但消息其实已经发出去 → 这就是重复发送的来源

# 3. 全量语法检查
python -m py_compile launcher.py runner.py daemon.py monitor.py gui.py reply_engine.py \
    send_v2.py verify_sent.py decrypt_image.py voice_text.py wxenv.py notify.py watch.py build_exe.py
```
