# 项目专属虚拟环境 .venv 的精确快照
# 重建：python -m venv .venv && .venv\Scripts\python.exe -m pip install -r requirements.lock.txt
# 注意：wechat-cli 不在 PyPI，装完下面这些后单独从本地目录装（路径含你的用户名，不入库）：
#   .venv\Scripts\python.exe -m pip install "C:\Users\<你的用户名>\.workbuddy\skills\wechat-local-butler\tool"
click==8.5.0
comtypes==1.4.16
pycryptodome==3.23.0
pywin32==312
uiautomation==2.0.29
zstandard==0.25.0
